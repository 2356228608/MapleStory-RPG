function enter(pi) {
    if (pi.isQuestFinished(24002) && !pi.isQuestActive(24093)) {
        //pi.warp(101050100, 0);
    } if (pi.isQuestActive(24040)) {
        pi.warp(101050000, 0);
    } else {
        //pi.warp(101050100, 0);
    }
}